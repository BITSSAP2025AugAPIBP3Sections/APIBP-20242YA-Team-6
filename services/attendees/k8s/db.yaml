apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: attendees-db-pvc
  namespace: event-management
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: attendees-db
  namespace: event-management
spec:
  replicas: 1
  selector:
    matchLabels:
      app: attendees-db
  template:
    metadata:
      labels:
        app: attendees-db
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: POSTGRES_DB
              value: attendees
            - name: POSTGRES_USER
              value: attendees
            - name: POSTGRES_PASSWORD
              value: attendeespw
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: attendees-db-pvc